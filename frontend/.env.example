# Only VITE_* variables are exposed to the browser — never put STM keys here.
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# STM iBUS: configure the backend only — see ../backend/.env.example